events {
  worker_connections 4096;
}

http {
  upstream backend {
    server backend:3000;
  }

  server {
    listen 80;
    listen [::]:80;
    listen 8080;
    listen [::]:8080;

    location / {
      proxy_pass http://backend/;
    }
  }

}
