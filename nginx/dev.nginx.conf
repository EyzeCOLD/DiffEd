events {
  worker_connections 4096;
}

http {
  upstream frontend {
    server frontend:3000;
  }

  server {
    listen 8080;
    listen [::]:8080;

    location / {
      proxy_pass http://frontend/;
    }
  }
}
